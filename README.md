# Stadia-X

Ingest, extract, resolve, persist, and query a corpus of policy documents and
standards down to the paragraph / clause / standard level.

See `docs/` for the architecture and build plan.

## Setup

```bash
uv sync
cp .env.example .env   # then fill in your keys
```

## Notes

- Use `uv run python`, never bare `python`.
