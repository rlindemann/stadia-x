# Copy to .env and fill in. config.py loads this automatically (existing env wins).
STADIA_FUSEKI_BASE=http://localhost:3030
STADIA_DATASET=stadia
STADIA_FUSEKI_USER=fuseki
STADIA_FUSEKI_PASSWORD=change-me
STADIA_ONTOLOGY_BASE=https://stadia.example/ontology#
STADIA_INSTANCE_BASE=https://stadia.example
STADIA_MODEL=claude-sonnet-4-5

# Needed only for --llm extraction
ANTHROPIC_API_KEY=
